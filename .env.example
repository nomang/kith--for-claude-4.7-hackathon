# ── Required ───────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=AI...

# ── Optional: Vercel KV (for persistent notebook + conversation storage) ───
# Create a KV store at vercel.com/dashboard → Storage → KV
# Then copy the connection strings here (or add via Vercel dashboard)
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
KV_URL=
